"use client";
import { Area, AreaChart, Bar, BarChart, CartesianGrid, Cell, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
const euro = (value: number) => `${Math.round(value).toLocaleString("de-DE")} €`;
export function AnalyticsCharts({ monthly, properties, utility }: { monthly: Array<{ month: string; due: number; paid: number }>; properties: Array<{ name: string; rent: number; rentPerSqm: number; occupied: number; units: number }>; utility: Array<{ name: string; value: number }> }) {
  return <div className="analytics-chart-grid"><section className="detail-panel chart-card wide"><div className="panel-title"><h2>Soll und Zahlungseingang</h2><span>12 Monate</span></div><ResponsiveContainer width="100%" height={280}><AreaChart data={monthly}><defs><linearGradient id="paid" x1="0" y1="0" x2="0" y2="1"><stop offset="5%" stopColor="#2477ff" stopOpacity={.35}/><stop offset="95%" stopColor="#2477ff" stopOpacity={0}/></linearGradient></defs><CartesianGrid strokeDasharray="3 3" opacity={.18}/><XAxis dataKey="month"/><YAxis tickFormatter={euro}/><Tooltip formatter={(value) => euro(Number(value))}/><Area type="monotone" dataKey="due" name="Soll" stroke="#8aa0bd" fill="transparent"/><Area type="monotone" dataKey="paid" name="Eingang" stroke="#2477ff" fill="url(#paid)"/></AreaChart></ResponsiveContainer></section><section className="detail-panel chart-card"><div className="panel-title"><h2>Miete je Objekt</h2><span>€/m²</span></div><ResponsiveContainer width="100%" height={270}><BarChart data={properties} layout="vertical"><CartesianGrid strokeDasharray="3 3" opacity={.18}/><XAxis type="number"/><YAxis dataKey="name" type="category" width={90}/><Tooltip formatter={(value) => `${Number(value).toFixed(2)} €/m²`}/><Bar dataKey="rentPerSqm" name="Miete je m²" fill="#2477ff" radius={[0,6,6,0]}/></BarChart></ResponsiveContainer></section><section className="detail-panel chart-card"><div className="panel-title"><h2>Betriebskosten</h2><span>Top-Kostenarten</span></div>{utility.length ? <ResponsiveContainer width="100%" height={270}><PieChart><Pie data={utility} dataKey="value" nameKey="name" innerRadius={55} outerRadius={95} paddingAngle={2}>{utility.map((_, index) => <Cell key={index} fill={["#2477ff","#00bfa6","#8b5cf6","#f59e0b","#ef6173","#38bdf8","#84cc16","#64748b"][index % 8]} />)}</Pie><Tooltip formatter={(value) => euro(Number(value))}/></PieChart></ResponsiveContainer> : <p className="muted">Noch keine Betriebskosten erfasst.</p>}</section></div>;
}
